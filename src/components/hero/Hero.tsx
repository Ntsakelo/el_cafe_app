import Effect from '../../Effect.svg';

export const Hero = () => {
    
    return (
        <div className="hero">
             <div className="home_hero">
                {/* <img className="slideImage" src={`${process.env.PUBLIC_URL}/starterCampaign.jpg`} alt="" />
                <img src={Effect} alt="" className="effect"/> */}
             </div> 
        </div>
    )
}
